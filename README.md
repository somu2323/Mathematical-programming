Libraries commonly used in this project
1.Django-web framework for building the project
  pip install Django
2.NumPy - Array operations, mathematical computations
  pip install numpy
3.Matplotlib - Plotting graphs and shading the feasible region.
  pip install matplotlib
4.SciPy - Used for Simplex method calculations (if applicable).
  pip install scipy
